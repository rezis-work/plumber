# Design tokens (mobile)

**Source of truth:** Keep this file in sync with [`apps/web/src/lib/design/tokens.json`](../../web/src/lib/design/tokens.json). Regenerate from Stitch (Design System / Caucasus Blue) via the web app workflow, then copy here.

Semantic colors for React Native are exposed through [`../theme/colors.ts`](../theme/colors.ts), aligned with [`apps/web/src/app.css`](../../web/src/app.css).
